import * as assert from 'assert';
import {Builder} from '../lib/builder';

describe('flem', () => {
  
  it('should build the sample node.js app', (done) => {
    //assert.equal(stats.size, size);
    let builder = new Builder();
    done();
  });
  
});